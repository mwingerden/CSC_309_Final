
import java.awt.*;
/**
 * An interface used for all instances where a draw method will be implemented.
 */
public interface Draw {

    /**
     * Draw the block to the passed in graphics object.
     * @param g
     */
    void draw(Graphics g);
}